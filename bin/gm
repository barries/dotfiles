#!/usr/bin/env bash

set -eo pipefail
shopt -s nullglob

if [[ $# -eq 0 ]]; then
    BRANCH=`git branch --all | perl -pe 's/^\\h+//; s/\\h+$//;' | fzf`
else
    BRANCH="origin/$1"
fi

git fetch && git merge "$BRANCH"
