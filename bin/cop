#!/usr/bin/env bash

set -eo pipefail
shopt -s nullglob

if [[ $# -eq 0 ]]; then
    BRANCH=`git branch --all | perl -pe 's/^\\h+//; s/\\h+$//;' | fzf`
else
    BRANCH="${@##*/}"
fi

git fetch && git checkout "$BRANCH" && git pull
