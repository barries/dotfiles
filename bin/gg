#!/usr/bin/env bash

set -eo pipefail
shopt -s nullglob

FORMAT="%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%ci, %cr) %C(bold blue)<%an>%Creset"

git log --graph --abbrev-commit --date-order "--pretty=format:$FORMAT" "$@"
